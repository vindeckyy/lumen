# lumen_api — Python tooling for the Lumen REST surface.
#
# This package ships the OpenAPI spec, the migrate tooling reference
# implementations, and the test entry point used in CI. It is *not*
# a Python wrapper around the Lumen binary — that's what lumenctl is.